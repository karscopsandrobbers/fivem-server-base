# The stock chat resource ships inside FXServer (citizen/system_resources/chat on Legacy) and is
# what the base talks through. Copy it into resources/ if your build does not find it.
ensure chat

# The base resource, last: it expects everything else to be up.
ensure base
