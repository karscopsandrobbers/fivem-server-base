# Content packs, one resource each. Drop a pack into resources/[dlc]/dlc_<name>/stream (and its
# .meta files into data/), run `npm run dlc` inside base/, and the manifest and the ensure line
# below are generated for you. See docs/dlcpacks.md.
#
# Do not edit between the markers: the generator owns that block.
# >>> dlc packs — generated by base/scripts/dlc.js
# <<< dlc packs
