/* Entity-level systems: the objective marker, and whatever you hang on every entity next. */
export { setObjectiveMarker, removeObjectiveMarker } from './objective_markers';
export type { ObjectiveMarkerData } from './objective_markers';
