# typescript-eslint's peer range does not list TypeScript 7 yet, so `typescript` is aliased to
# @typescript/typescript6 for it. This flag keeps `npm install` and `npm ci` resolving the same tree.
legacy-peer-deps=true
